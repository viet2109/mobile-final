import React from 'react'
import { Text, View } from 'react-native'

interface Props {}

function Send(props: Props) {
    const {} = props

    return (
        <View>
            <Text>Send</Text>
        </View>
    )
}

export default Send
